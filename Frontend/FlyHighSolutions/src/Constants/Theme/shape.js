import { createTheme } from "@mui/material/styles";

export const themeShape = createTheme({
  shape: {
    standard: {
      borderRadius: "8px",
    },
    standard_small: {
      borderRadius: "4px",
    },
  },
});